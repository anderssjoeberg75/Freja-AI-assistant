# SQLite databases (contains secret tokens/keys)
keys.db
freja.db
database.db
*.db-shm
*.db-wal

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage
htmlcov/
test_venv/
.venv/
venv/

# OS/System
.DS_Store
Thumbs.db

# Workspace / temp / caches
scratch/
.garminconnect/
.telegram_bot.lock
backend/cache/

# Secret keys and local state/downloads
facebook_state.json
downloads/
.freja_secret.key
.env
.env.*

# Generated codex artifacts (audit reports, auto-fix backups)
docs/code_audit_*.md
*.bak
*.codex_backup*

# Database backups and migration bundles (contain secrets - never commit)
keys.db.bak-*
*.db.bak-*
migration_to_server/

# Graphify knowledge graph (generated locally; rebuilt via git hooks, no need to commit)
graphify-out/

